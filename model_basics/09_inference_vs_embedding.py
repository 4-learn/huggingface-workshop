"""
Workshop 解答：Inference vs Embedding

題目：
針對以下需求，判斷應該用 Inference 還是 Embedding：
1. 生成違規告警訊息
2. 找出相似的歷史違規
3. 判斷違規嚴重程度
4. 搜尋相關法規條文
5. 摘要本週違規報告

執行方式：
python 09_inference_vs_embedding.py
"""


def main():
    print("=== Inference vs Embedding 選擇指南 ===\n")

    # === 概念複習 ===

    concepts = """
概念複習
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【Inference（推論）】
輸入文字 → 模型 → 輸出文字/分類結果
- 生成新的文字內容
- 做出判斷或決策
- 回答問題

【Embedding（向量化）】
輸入文字 → 模型 → 數字向量
- 將文字轉換為可比較的向量
- 用於相似度搜尋
- 用於聚類分析
"""
    print(concepts)

    # === 需求分析表 ===

    analysis = """
需求分析
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌────────────────────┬────────────┬────────────────────────────────┐
│ 需求                │ 選擇       │ 理由                            │
├────────────────────┼────────────┼────────────────────────────────┤
│ 1. 生成違規告警訊息  │ Inference  │ 需要「生成」新的文字內容         │
│                    │            │ 模型需要理解違規情境並產出描述    │
├────────────────────┼────────────┼────────────────────────────────┤
│ 2. 找出相似的歷史違規│ Embedding  │ 需要「比較」文字的語意相似度      │
│                    │            │ 將違規描述轉為向量後計算距離      │
├────────────────────┼────────────┼────────────────────────────────┤
│ 3. 判斷違規嚴重程度  │ Inference  │ 需要「判斷」並輸出分類結果        │
│                    │            │ 模型需要理解內容並做出決策        │
├────────────────────┼────────────┼────────────────────────────────┤
│ 4. 搜尋相關法規條文  │ Embedding  │ 需要「搜尋」語意相關的內容        │
│                    │            │ 將查詢和法規都向量化後比對        │
├────────────────────┼────────────┼────────────────────────────────┤
│ 5. 摘要本週違規報告  │ Inference  │ 需要「生成」摘要文字             │
│                    │            │ 模型需要理解並濃縮內容           │
└────────────────────┴────────────┴────────────────────────────────┘
"""
    print(analysis)

    # === 詳細說明 ===

    details = """
詳細說明
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【1. 生成違規告警訊息 → Inference】

```python
# Inference 做法
prompt = "為以下違規生成告警訊息：工人在A區未戴安全帽"
response = llm.generate(prompt)
# 輸出：「【工安警報】A區偵測到人員未配戴安全帽，請立即改正...」
```

為什麼不用 Embedding？
- Embedding 只能把文字變成數字
- 無法「產生」新的文字內容

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【2. 找出相似的歷史違規 → Embedding】

```python
# Embedding 做法
query = "工人沒戴安全帽"
query_vec = embedding_model.encode(query)

# 與歷史違規比較
for violation in history:
    vec = embedding_model.encode(violation)
    similarity = cosine_similarity(query_vec, vec)
    if similarity > 0.8:
        print(f"相似違規：{violation}")
```

為什麼不用 Inference？
- Inference 無法有效比較大量資料
- Embedding 可以預先計算，搜尋更快

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【3. 判斷違規嚴重程度 → Inference】

```python
# Inference 做法
prompt = '''
判斷以下違規的嚴重程度（高/中/低）：
違規描述：工人在高空作業時未繫安全帶
'''
response = llm.generate(prompt)
# 輸出：「高」
```

為什麼不用 Embedding？
- 判斷需要「推理」能力
- 需要理解「高空」+「未繫安全帶」=「高風險」

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【4. 搜尋相關法規條文 → Embedding】

```python
# Embedding 做法（RAG 的核心）
query = "安全帽規定"
query_vec = embedding_model.encode(query)

# 搜尋法規向量資料庫
results = vector_db.search(query_vec, top_k=3)
# 返回最相關的 3 條法規
```

為什麼不用 Inference？
- 法規庫可能有上萬條
- 用 Inference 逐條問太慢且成本高
- Embedding 搜尋可以毫秒級完成

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【5. 摘要本週違規報告 → Inference】

```python
# Inference 做法
prompt = f'''
請摘要以下本週違規報告：
{weekly_report}

輸出 3 個重點：
'''
response = llm.generate(prompt)
# 輸出：「1. 本週共 23 件違規... 2. PPE違規佔 65%... 3. A區違規最多...」
```

為什麼不用 Embedding？
- 摘要需要「理解」內容並「生成」新文字
- Embedding 無法生成文字
"""
    print(details)

    # === 決策樹 ===

    decision_tree = """
決策樹：如何選擇？
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

                    需要什麼？
                        │
            ┌───────────┴───────────┐
            │                       │
        生成文字？              比較/搜尋？
            │                       │
            ▼                       ▼
       Inference               Embedding
            │                       │
    ┌───────┴───────┐       ┌───────┴───────┐
    │       │       │       │               │
   生成    判斷    摘要    相似度搜尋      聚類分析
   文字    分類    提取    向量比對        分群
"""
    print(decision_tree)

    # === 常見組合 ===

    combinations = """
常見組合：RAG（Retrieval-Augmented Generation）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

RAG = Embedding（搜尋）+ Inference（生成）

```
用戶問題：安全帽的規定是什麼？
    │
    ▼
Embedding 搜尋法規庫
    │
    ▼
找到相關法規條文
    │
    ▼
Inference 根據法規回答問題
    │
    ▼
輸出：根據職業安全衛生設施規則第 280 條...
```

這樣結合的好處：
1. Embedding 確保找到正確資料
2. Inference 生成流暢的回答
3. 減少幻覺問題
"""
    print(combinations)

    print("=== 完成 ===")


if __name__ == "__main__":
    main()
